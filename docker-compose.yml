services:
  cliponomicon:
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL}
    container_name: cliponomicon
    env_file:
      - path: stack.env
        required: false
      - path: .env
        required: false
    ports:
      - "8500:80"
    restart: unless-stopped
