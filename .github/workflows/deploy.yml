name: Deploy Luigi Portainer

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Portainer webhook
        run: curl -k -X POST "${{ secrets.PORTAINER_WEBHOOK_URL }}"